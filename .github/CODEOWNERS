# Code owners for command-center-starter.
#
# The `main` ruleset requires a review from a code owner on every pull request.
# More than one owner is listed on purpose: GitHub does not let an author's own
# ownership satisfy that requirement, so a single-owner file would permanently
# block that person's own PRs.

*   @AndreaSucreG @felixcarpena @raquelyuste
