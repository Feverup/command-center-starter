# esbuild (via Vite) needs its postinstall to fetch the platform binary; pnpm 11
# blocks install scripts unless allowed here.
allowBuilds:
  esbuild: true
minimumReleaseAgeExclude:
  - '@asucregonzalez/section-delivery-projects@0.2.0'
